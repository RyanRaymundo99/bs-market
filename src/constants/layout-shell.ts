/** Left padding so in-flow content clears the fixed desktop sidebar (`w-56`). */
export const DESKTOP_SHELL_PL = "md:pl-56";

/** Bottom padding on mobile so content clears the fixed tab bar + safe area. */
export const MOBILE_BOTTOM_NAV_PADDING =
  "pb-[calc(5rem+env(safe-area-inset-bottom,0px))] md:pb-0";